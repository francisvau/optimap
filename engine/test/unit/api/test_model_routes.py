# from fastapi.testclient import TestClient


# def test_get_all_models_empty(client: TestClient):
#     response = client.get("/ai-models/")
#     assert response.status_code == 200
#     assert response.json() == {"models": []}
